{
  "name": "@indenture/validator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Edge Validator: re-derives every fact from source, signs or refuses. Holds VALIDATOR_KEY.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:vercel": "vercel deploy --prod",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "dev:node": "tsx src/serve-local.ts"
  },
  "dependencies": {
    "@indenture/chainlink": "*",
    "@indenture/hedera": "*",
    "@indenture/mandate": "*",
    "@indenture/receipt": "*",
    "hono": "^4.6.0",
    "viem": "^2.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0",
    "tsx": "^4.19.0"
  }
}
